This project installs and mounts a Lustre file system.

There are two steps to the process:
1. Install the amlfs client package using install-amlfs-client-package.sh
2. Create mount and add to /etc/fstab using add-lustre-mount.sh